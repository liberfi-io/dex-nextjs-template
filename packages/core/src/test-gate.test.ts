describe("package TypeScript test gate", () => {
  it("transforms TypeScript before execution", () => {
    const packageName: string = "@liberfi/core";

    expect(packageName).toBe("@liberfi/core");
  });
});
