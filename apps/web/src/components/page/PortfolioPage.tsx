/**
 * The PortfolioPage implementation lives under `./portfolio/`. This file
 * is preserved as a thin re-export so existing route imports
 * (`app/(new)/portfolio/page.tsx`, etc.) keep working without touching
 * the Next.js routing tree.
 */
export { PortfolioPage } from "./portfolio/PortfolioPage";
